      integer MaxAng, Observed, Actual
      parameter(MaxAng=1000)
      parameter(Observed=1)
      parameter(Actual=2)

      real RadperAM, radtoasec, minTheta
      parameter(RadperAM=2.90888208e-4)
      parameter(radtoasec=206264.806247)
      parameter(minTheta=1.0e-3)

